# Day 35 - Intersection Of Two Arrays

## ðŸ§© Problem
Return an array of their intersection (each element appears only once).

## ðŸ’¡ Approach
- Store first array elements in HashSet
- Iterate second array and add matches to result set
- Convert result set to array

## â±ï¸ Complexity
- Time: O(m + n)
- Space: O(min(m, n))
